<h1>Choose Your Product From Listed Categories</h1>

<ol>
    <% @categories.each do |category| %>
        <li>
            <strong><%= link_to category.title, category_products_path(category) %></strong>(<%= category.products.length %> products)
        </li>
    <% end %>
</ol>